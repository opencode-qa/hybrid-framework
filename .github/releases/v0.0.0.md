---
title: "Project Skeleton & CI Setup"
assignees:
  - opencode-qa
  - Anuj-Patiyal
reviewers:
  - Anuj-Patiyal
labels:
  - release
  - setup
  - ci-cd
  - core
milestone: "v0.0.0 - Framework Bootstrap & CI Setup"
linked_issue: 1
---

# 🎯 Release: Project Skeleton & CI Setup (`v0.0.0`)

{{RELEASE_METADATA}}

## 📌 Summary
This release officially establishes the strict CI/CD architecture and Maven foundation for the **Playwright Java Hybrid Automation Framework** in the `main` branch. It securely locks in the GitHub Actions, repository rules, and bash automation scripts required to maintain enterprise-grade code quality.

## 🛠 Key Changes
- [x] Initialized Maven project (`pom.xml`) with `Java 21` minimums.
- [x] Integrated `pom-validator.sh` (v2.4) with HTML reporting.
- [x] Configured strict GitHub Actions pipelines (`main-ci.yml`, `feature-pr.yml`, `release-pr.yml`).
- [x] Set up automated PR, Milestone, and Issue management scripts.
- [x] Established basic repository directory layout.

## ✅ Quality Assurance
- Verified custom POM validation passes strict mode.
- Confirmed all GitHub Action runners execute successfully.
- Validated automation script syntax and execution permissions.

## 📝 Release Notes
This PR promotes `v0.0.0` from `dev` to `main`. After merging, GitHub Actions will:
1. Create a signed Git tag `v0.0.0`
2. Generate a GitHub Release with auto‑notes
3. Bump `dev` to `v0.1.0-SNAPSHOT` (next minor)
4. Open a version bump PR (`chore/bump-to-v0.1.0`)

## 🔄 Post-Merge Automation
The following will happen automatically (via `release-pr.yml`):
- Tag `v0.0.0` created on `main`
- GitHub Release published
- `dev` branch receives the next snapshot version
