---
title: "🎯 v0.0.0: Project Skeleton & CI Setup"
assignees:
  - opencode-qa
  - Anuj-Patiyal
reviewers:
  - Anuj-Patiyal
labels:
  - setup
  - ci-cd
  - core
  - enhancement
milestone: "v0.0.0 - Framework Bootstrap & CI Setup"
linked_issue: 1
---

# 🎯 feat: Project Skeleton & CI Setup (`v0.0.0`)

{{DYNAMIC_METADATA}}

## 📌 Summary
This PR introduces the foundational setup for the **Playwright Java Hybrid Automation Framework**. It establishes the strict CI/CD gates, automated scripts, and the initial Maven scaffolding required before introducing testing dependencies in the next phase.

## 📂 Files Introduced or Modified
```txt
📦 playwright-hybrid-framework/
├── 📄 pom.xml              # Base Maven config enforcing Java 21 (🆕)
├── 📁 .github/workflows/   # main-ci.yml, feature-pr.yml, release-pr.yml (🆕)
├── 📁 scripts/             # pom-validator.sh, milestones.sh, issues.sh, feature-pr.sh, release-pr.sh (🆕)
├── 📄 .gitignore           # Standard ignores for Java/Maven (🆕)
└── 📄 README.md            # Project overview and setup guide (🆕)
```
## 🎯 Key Features Introduced
- ✅ Maven Initialization: Empty dependency tree, strict `Java 21` enforcement.
- ✅ CI/CD Pipelines: Complete GitHub Actions architecture.
- ✅ Custom Validation: `pom-validator.sh` `v2.4` with Dark Mode HTML reports.
- ✅ Repository Automation: Feature, Release, and Issue automation scripts.

## ✅ Task Checklist (Issue #1)
- Maven `pom.xml` with `Java 21`, groupId `opencode-qa`, artifactId  `hybrid-framework`, version `0.0.0-SNAPSHOT`
- GitHub Actions: `main-ci.yml`, `feature-pr.yml`, `release-pr.yml`
- Automation scripts: `pom-validator.sh`, `milestones.sh`, `issues.sh`, `feature-pr.sh`, `release-pr.sh`
- Repository structure: `src/main/java`, `src/test/java`, `scripts/`, `.github/`
-  `.gitignore` for Java/Maven/IDE
- Basic `README.md`

## 🛠️ How to Verify
1. **Run Maven validation locally:**

```bash
./scripts/pom-validator.sh --strict --html
```

2. Verify CI/CD execution: Check the Actions tab to ensure Main CI Pipeline passes successfully.
3. Ensure pom.xml passes xmllint and pom-validator.sh with zero failures.

## 🚧 Next Steps
- Merge this PR into dev
- Open Release PR: `main` ← `dev` using ./scripts/release-pr.sh
- Tag initial release: `v0.0.0`
